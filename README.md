# My temporary home

SNU Finntech landing page — simple frontend prototype.

Run locally with live reload:

1. Install dependencies: `npm install`
2. Start the dev server: `npm start` (opens http://127.0.0.1:5173)

Or open the `Start Live Server` VS Code task from the Command Palette (Tasks: Run Task) or install the Live Server extension and use it directly.
